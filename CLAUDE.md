# Public ArchFlow Route

Use `AGENTS.md` in this folder first.

All current project work after the 2026-06-24 reset belongs under `project/`.
History is summary-only under `history/`.

Keep this file short. Long instructions belong in `project/operating-rules.md`.
