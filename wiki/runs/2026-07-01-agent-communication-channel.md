# 2026-07-01 Agent Communication Channel

## Task

Create a live communication folder for ArchFlow public project agents and provide a system prompt append for the new coordination pattern.

## Result

- Added `project/live/communication/` as the shared public-safe channel for active parallel agent dialogue.
- Added the current notice, message template, pattern-change log, and copy-ready system prompt append.
- Updated `project/operating-rules.md`, `project/agents/README.md`, and `project/agents/agent-roster.yaml` so future agents route to the live communication channel before work.
- Recorded this handout under `project/runs/2026-07-01-agent-communication-channel/`.

## Follow-Up

Future agents should append starting, running, blocked, handoff, and complete updates to `project/live/communication/agent-communication-log.md` and keep substantial completion records under `project/runs/`.
