# Current Agent Notice

Date: 2026-07-01
Status: active

All ArchFlow public project agents must use `project/live/communication/` as the shared live coordination channel.

Before starting or continuing work:

1. Read `project/live/communication/README.md`.
2. Read the latest entries in `project/live/communication/agent-communication-log.md`.
3. Append a `starting` update before editing files.
4. Name the files you expect to touch.
5. Check whether another agent has claimed the same files.

If another agent is working on the same file or task, stop and coordinate in `agent-communication-log.md` before editing.

For substantial work, still create or update the relevant run handout under `project/runs/`. The live communication folder coordinates active work; the run handout records durable completion state.

Safety boundary: this folder is public-safe only. Do not paste secrets, raw private source material, local absolute paths, private URLs, account IDs, screenshots, credentials, or personal identifiers.
