# E1.2.8 OpenRouter Blocked

Status: blocked

Reason: OpenRouter comparison was attempted only with the sanitized digest, but the first run hit sandbox DNS/network blocking. The escalation reviewer then rejected the external provider call as a data-exfiltration risk for derived content from private local source materials.

No workaround was attempted. No raw source text, key, credential, or provider response is stored here.

Next safe action: owner must explicitly approve the external OpenRouter comparison after reviewing the risk, or keep E1.2.8 accepted as a local/Codex-only package.
