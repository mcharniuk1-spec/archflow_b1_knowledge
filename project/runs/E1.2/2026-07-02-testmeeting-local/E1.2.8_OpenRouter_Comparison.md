# E1.2.8 OpenRouter Provider Comparison

Date: 2026-07-03T08:32:43.098745+00:00
Provider: OpenRouter
Model: qwen/qwen3.6-plus
Model selection: first_available_yushchenko_source_scope_execution_model
Route: yushchenko.source_scope_gate
Input kind: sanitized digest only
Raw private source sent: false

# Provider Output Summary
- **Execution Route:** `source_scope_gate` -> `prd_blocks_graph` -> `risk_gate_graph`
- **Model:** `qwen/qwen3.6-plus` (primary execute), with `qwen/qwen3-235b-a22b` as fallback. Selection driven by role/node-based routing, not epic-based.
- **Input Scope:** Strictly limited to sanitized digest payloads (`SRC-E128-MEETING-SUMMARY`, `SRC-E128-DISCOVERY-METHOD`, `SRC-E1-PUBLIC-STATE`). Raw private source material was excluded from the provider call.
- **Output State:** Structured scope extraction and PRD comparison artifacts generated within the `source_scope_gate` node. Output remains gated pending ledger validation and AF Review before any frontier model routing or external delivery.

# PRD Delta Versus Local Codex Run
- **Local Codex Baseline:** Marked `ready_or_review`. Contains the approved E1.2.8 testmeeting architecture, local dashboard selector, persistent browser-local chat, and provider-disabled API contract.
- **Provider Run Delta:** Introduces explicit governance gating (`blocked_or_gated`) for external provider comparison until the sanitized payload and ledger pass. The provider path enforces a structured digest-first workflow, whereas the local Codex run operates directly on browser-local state.
- **Structural Alignment:** Both paths target the same E1.2.8 architecture and service direction (AI-native PRD automation for B2B SaaS product teams). The provider delta adds explicit boundary enforcement and pre-review gating before any downstream PRD block generation.

# Evidence And Boundary Review
- **Input Kind:** `sanitized_digest_only`
- **Raw Private Source Sent:** `false`
- **Redacted Identifiers:** Private source filenames, personal names, private URLs, Notion URLs, workspace/account/project identifiers, credentials, raw transcript text, raw DOCX body.
- **Boundary Enforcement:** The `source_scope_gate` node strictly filters inputs to approved digest IDs. No raw transcripts, DOCX bodies, or workspace credentials are exposed to the external provider.

# Task/Backlog Improvements
- **Immediate Tasks:** 
  1. Finalize sanitized payload ledger validation.
  2. Execute AF Review on the provider-generated PRD comparison packet.
  3. Align provider output structure with the local Codex PRD baseline.
  4. Prepare `risk_gate_graph` transition criteria.
- **Backlog Items (Gated):** 
  - Durable writeback implementation
  - Railway hosted runtime provisioning
  - Production promotion checklist
  - External delivery channels (Telegram, Notion writeback) remain explicitly out of scope until gates clear.

# Risks And Gaps
- **Blocked/Gated States:** External provider comparison, Railway hosted runtime, durable writeback, and production promotion are explicitly blocked until ledger and AF Review gates pass.
- **Ready/Review States:** Local Codex PRD package, local dashboard architecture selector, persistent browser-local chat, proactive interview start, and provider-disabled local API contract are cleared for review.
- **Operational Constraint:** The system is intentionally constrained to local/browser execution. Advancing to hosted runtime or external writeback requires explicit gate clearance and compliance validation.

# Recommendation
- Maintain the local Codex PRD package as the authoritative baseline until the sanitized payload ledger passes and AF Review completes.
- Proceed with `source_scope_gate` -> `prd_blocks_graph` execution using `qwen/qwen3.6-plus` strictly on the sanitized digest.
- Do not advance to Railway deployment, durable writeback, Telegram delivery, or Notion integration until all `blocked_or_gated` items are explicitly cleared.
- Focus immediate effort on finalizing the ledger pass, completing the AF Review, and documenting the `risk_gate_graph` acceptance criteria.

---

### FACT / INTERPRETATION / HYPOTHESIS / GAP Separation

**FACT**
- The payload is a `sanitized_external_provider_payload` routed through `source_scope_gate`.
- Raw private source material was not sent; only sanitized digests (`SRC-E128-*`) were used.
- External provider comparison, Railway runtime, durable writeback, and production promotion are explicitly `blocked_or_gated`.
- Local Codex PRD, dashboard selector, browser-local chat, and provider-disabled API contract are `ready_or_review`.
- Service direction targets AI-native PRD automation for B2B SaaS product teams (Series B-D, 50-500 employees, 2-5 PMs).

**INTERPRETATION**
- The `source_scope_gate` enforces a strict digest-only boundary to prevent private data exposure during external provider execution.
- The blocked/gated status reflects intentional governance controls, not system failure; progression requires ledger validation and AF Review.
- The local vs. provider delta highlights a transition from direct local context to a governed, sanitized pipeline before any downstream PRD generation.

**HYPOTHESIS**
- Passing the sanitized payload ledger and completing AF Review will safely unblock external provider comparison without compromising source boundaries.
- Maintaining the provider-disabled local API contract as a fallback will preserve system stability during the transition to hosted runtime.
- Structured digest extraction will scale to frontier models once the `risk_gate_graph` criteria are formally defined and met.

**GAP**
- No explicit acceptance criteria or validation steps are documented for the `risk_gate_graph` transition.
- Durable state persistence outside browser-local storage is not yet implemented or validated.
- Ledger pass mechanics and AF Review sign-off thresholds are pending definition before external comparison can proceed.
