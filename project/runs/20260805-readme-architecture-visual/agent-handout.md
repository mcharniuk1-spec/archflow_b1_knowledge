# Agent Handout — README Architecture Visual

## Purpose

This run integrates the owner-supplied ArchFlow architecture illustration into the public GitHub README and adds an accessible explanation of the system, its connections, and its fictional Ukrainian role call names.

## Human summary

The root README now uses a cleaned version of the supplied layered architecture image as its visual introduction. The surrounding Markdown explains every region of the illustration, all seven operating layers, and the meaning of the blue, violet, coral, gold, and return connections.

All 21 canonical roles now have fictional Ukrainian call names written in English letters. The exact technical role ID remains next to each name, and the README states that a display name cannot grant authority or replace the machine contract.

The independent reviewer issued APPROVE after checking the image, captions, role coverage, public boundary, architecture parity, accessibility, and runtime-claim calibration. No dashboard or runtime implementation was changed.

## Current state

- Documentation admission accepted with provider execution disabled.
- Image cleanup used the built-in image editing path; the source composition is preserved and the cropped top-edge artifacts are removed.
- README integration and independent review are complete.
- Deterministic role, link, image, architecture-fixture, and whitespace checks pass.
- Exact staging, staged-snapshot safety, commit, push, and remote readback remain in progress.

## Continuation boundary

Continue only within `README.md`, `project/assets/architecture/archflow-knowledge-process.png`, and this run folder. Preserve every unrelated worktree change. Do not edit the dashboard, activate providers, deploy, or treat display call names as role authority.

## Execution trace

FACT: Documentation admission was accepted after two bounded request repairs. The built-in image editing path removed the cropped top-edge artifacts. README/catalog comparison found 21 canonical roles and 21 unique call names. Independent review returned APPROVE.

INTERPRETATION: Keeping exact role IDs beside friendly call names provides human readability without weakening authorization consistency.

GAP: Reader comprehension has not yet been measured in an employee onboarding pilot.

## Decisions

- Keep all labels and captions in Markdown rather than embedding tiny text in the PNG.
- Use one fictional Ukrainian call name per canonical role.
- Preserve the catalog IDs and authority contract unchanged.
- Keep dashboard and runtime implementation out of scope.

## Artifacts

- `README.md` — GitHub-facing architecture explanation and roster.
- `project/assets/architecture/archflow-knowledge-process.png` — cleaned conceptual hero.
- `independent-review.md` — read-only APPROVE verdict.
- `execution-report.md` — execution and validation ledger.

## Validation

- Image integrity and visual readback: PASS, 1586 × 992 RGB PNG.
- Canonical role coverage: PASS, 21 of 21 exactly once.
- Fictional call-name coverage: PASS, 21 unique Latin-letter names.
- README relative links: PASS.
- Architecture validator: PASS, provider disabled, zero actions executed.
- Independent review: APPROVE.
- Exact staged-snapshot safety: PASS after final staged-content scan.

## Next actions

1. Stage only the claimed files and exact append-only log hunks.
2. Run the public safety scanner and architecture validator from a clean staged snapshot.
3. Inspect the staged diff, commit, push under the owner's request, and read back the GitHub hash.

## Safety boundary

The image and documentation must remain public-safe. Do not add real employee identities, credentials, local paths, private URLs, source screenshots, or unsupported production claims.
