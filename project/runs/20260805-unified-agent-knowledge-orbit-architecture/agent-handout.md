# Agent Handout — Unified Agent Knowledge and Orbit Architecture

Status: implementation complete; independent review APPROVE; clean staged-snapshot checks PASS; commit pending

## Goal

Create one governed knowledge-agent operating architecture for employee onboarding, role-safe action support, and requirements-based validation; integrate Orbit privately with Obsidian; publish a reusable public system and documentation set; plan but do not implement dashboard integration.

## Admission

- Profile: `architecture`
- Provider mode: disabled
- Receipt: sha256:395aa2711dff1e4a7e9e20de951bbd2d25875f9696012d60374b97d025eaf4ac
- Admission status: valid for planning only; provider execution, task invocation, checkpoint writes, and external actions remained disabled.

## Roles and lanes

See `task-contract.md`. The integrator owns merge order, privacy boundary, shared-file edits, final validation, durable records, commit, and push.

## Current facts

- The public repository has unrelated pre-existing worktree changes. They are preserved and excluded from this run's staged set.
- Existing durable memory already positions PRDs as generated artifacts inside a broader Product Knowledge Reliability Setup.
- Dashboard implementation is out of scope; only an integration plan may be written.
- Orbit instructions were referenced by the owner but no attached prompt is present in the current workspace or message payload. The integration will therefore use discovered local/project evidence and verified first-party Orbit documentation, with any unresolved identity/config detail recorded as a GAP.

## Delivered architecture

- One versioned Knowledge Case Controller contract replaces numbered top-level architecture tracks.
- Requirements and market research are one bounded maker role; PRD, ICP, onboarding, outreach, and creative outputs are reviewed projections or role artifacts.
- A normalized role catalog preserves architecture, source/context, onboarding, research, qualification, copy, design, implementation, validation, verification, independent review, integration, knowledge, external action, release, outcome, observability, surface, and planned packaging responsibilities.
- The provider-disabled validator applies nested schemas and evaluates positive, stale, unknown-authority, target-escape, reviewer-spoof, and malformed cases without executing any proposal.
- Orbit is a private generated-evidence adapter over one clean snapshot of explicitly allowlisted public code paths. The constrained bridge exposes status and bounded repository maps only and checks exact corpus, clean snapshot, indexed commit, and source/snapshot hash freshness before every result.
- Obsidian retains Nexus, the constrained Orbit bridge, the adapted active `orbit` role contract, and a sanitized control note. Upstream skill guidance is preserved as non-active provenance reference. The vault itself is never an Orbit corpus.
- Public architecture, onboarding, adaptation, Orbit, role-trace, status/navigation, security, contribution, dashboard-plan-only, and decision documentation were added or updated.
- One conceptual layered image and two editable accessible SVG diagrams document the repository/system boundary and onboarding/action-validation flow.

## Role and agent execution record

**System architecture engineer.** Audited current and superseded contracts plus representative PRD/ICP, onboarding, market/pain, outreach, publication, design, review, and promotion runs. Saved the source register, unified state machine, canonical case packet, role catalog, validation contract, public/private seams, and dashboard migration phases in the system architecture lane. The codebase-design and Graphify routing discipline kept the controller interface small while preserving deep internal governance.

**Orbit and knowledge integration engineer.** Compared task-matched Orbit products using first-party behavior, selected GitLab Orbit Local at documented 0.80 confidence, and designed the fixed public-code structural-evidence seam. Saved corpus, storage, metadata, onboarding, action-validation, promotion, testing, and rollback rules in the Orbit lane. The upstream skill was treated as provenance; the active vault skill is the ArchFlow-specific bounded adaptation.

**Repository research and information architect.** Benchmarked LangGraph, LlamaIndex, CrewAI, Obsidian's sample plugin, Khoj, Dify, AnythingLLM, and Open WebUI from first-party sources. Saved 42 unique official links, reusable repository/documentation patterns, anti-patterns, test layout, and visual grammar in the benchmark lane. Deep-research QUICK was narrowed to source authority and current repository organization rather than broad market synthesis.

**Codex integrator and architecture designer.** Ran admission, reconciled all lanes, implemented the public schemas/fixtures/validator and private Orbit/Obsidian bridge, created the documentation/visual system, maintained public/private boundaries, updated durable memory, and owned exact release staging. Image generation supplied conceptual architecture art; deterministic SVG remained the canonical labeled diagram source.

**Independent architecture reviewer.** Performed maker-separated review and two bounded re-reviews. The first pass found fail-open authority, shallow lineage validation, and Orbit freshness/corpus defects; the second found exact-target and reviewer substitution gaps. Permanent adversarial fixtures and wrapper tests were added. The final verdict is APPROVE, conditioned on exact intended staging and a clean staged-snapshot safety pass.

## Verification

- Deterministic provider-disabled admission: PASS.
- Public JSON/schema definition and application checks: PASS.
- Provider-disabled five-proposal fixture plus malformed packet: PASS; one eligible, four blocked, none executed.
- Isolated copied-system proof with standard-library Python: PASS.
- Orbit v0.95.1 checksum/version, one-corpus index, clean snapshot, commit/hash freshness, bounded map, second/wrong/failed/stale/traversal denials: PASS.
- Constrained MCP initialize/list/status/map: PASS; exactly two safe tools, no raw SQL or `index`.
- Obsidian/Cursor configuration backups, JSON match, adapted skill/control-note readback: PASS; client reload/live discovery remains pending.
- SVG XML/accessibility and focused visual review: PASS after failure-path repair.
- Target documentation links: PASS.
- Target-only public safety: PASS; no target finding. Repository-wide scan still reports unrelated pre-existing artifacts.
- Dashboard code/API/data: not changed or staged by this run.

## Remaining gaps

- The missing owner attachment means GitLab Orbit Local remains the leading task-matched identity, not a certainty.
- Obsidian/Cursor must reload before live client discovery is claimed.
- Orbit is beta and requires pin/schema/upgrade re-verification.
- The public proof is synthetic, provider-disabled, and writeback-disabled; live employee usefulness and production behavior need separately approved pilots.
- Dashboard migration is documented but not executed.

## Next safe action

Stage only the intended public files, run the authoritative scanner against a clean staged snapshot, inspect the staged diff, commit, and use the owner's explicit Git-push authorization. Preserve all unrelated worktree changes.

## Expected outputs

- Unified operating architecture and role catalog.
- Requirements/market-research agent role.
- Employee onboarding and action-validation workflow.
- Private Orbit/Obsidian integration and verification evidence.
- Public clean-clone documentation and adaptation guide.
- Layered architecture visuals.
- Dashboard integration plan only.
- Independent review and public-safety receipt.
- Focused Git commit and push receipt.

## Constraints

No dashboard code changes, provider execution, deployment, production promotion, publication, outreach execution, private-source promotion, secret persistence, or uncontrolled ingestion.
