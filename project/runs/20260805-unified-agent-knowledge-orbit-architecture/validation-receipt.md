# Validation Receipt

Date: 2026-08-05
Release state: independent architecture APPROVE; clean staged-snapshot safety and system proof PASS; commit pending

## Public contract proof

- JSON contracts, schemas, and fixtures parse.
- The standard-library validator checks its supported schema definitions and applies them to nested packets.
- Controller states, canonical role IDs, aliases, evidence/requirement lineage, requirement currentness, exact target prefix, case maker/reviewer assignment, operation/class authority, side effects, approval, rollback, and readback are checked.
- One proposal is eligible. Stale external, unknown-authority, target-escape, and reviewer-spoof proposals are blocked. Malformed/escape schema packets are rejected. Every evaluated packet reports `executed: false`.
- The same proof passes from an isolated copied `project/system` directory without private files or dependencies.

## Orbit/private seam proof

- Pinned Orbit Local v0.95.1 archive checksum and binary version verified.
- One private database contains one exact clean snapshot of the allowlisted public code paths.
- Before status or map output, the wrapper checks database cardinality/path/status, clean snapshot state, indexed commit, and matching SHA-256 source/snapshot digest.
- Focused tests reject a second corpus, wrong corpus, failed index, stale digest, parent traversal, and unsupported map operation/value.
- MCP exposes `orbit_status` and `orbit_repo_map` only. Raw SQL and corpus-changing `index` are not exposed.
- The direct bridge is proven; Obsidian/Cursor live discovery remains reload-pending and is not claimed.

## Documentation and visuals

- Target relative links resolve.
- Both SVGs parse, contain accessible title/description text, and passed focused visual review.
- The repaired red failure path reaches repair/block directly and cannot be read as knowledge promotion.
- The conceptual image is an explanatory cover; editable SVG is canonical for labeled architecture.

## Safety and scope

- Target-only public-safety scan: PASS.
- Repository-wide scanner: FAIL only on unrelated pre-existing artifacts; none may enter the exact staged set.
- No dashboard implementation, provider execution, deployment, publication, outreach, private source promotion, or external action occurred.
- No generated bytecode, Orbit binary, database, snapshot, raw result, absolute path, credential, or private identifier is a public target.

## Independent verdict

APPROVE after bounded repairs and focused final re-review. Release remains conditional on exact intended staging, a clean staged-snapshot authoritative safety scan, staged-diff inspection, and successful owner-authorized push.
